// create by lipracer




class DecodeVide
{
public:
    DecodeVideo();
    DecodeVideo();
    DecodeVideo(DecodeVideo& other);
    DecodeVideo(DecodeVideo&& other);
    DecodeVideo& operator=(DecodeVideo&);
    DecodeVideo& operator=(DecodeVideo&&);
private:
public:
private:
    int m_width;
    int m_height;
    int depth;
    int m_buf_len;
    unsigned char *m_buf;


}
